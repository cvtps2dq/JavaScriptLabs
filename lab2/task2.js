function concatenateChars(char1, char2, char3) {
    let concatenatedString = char1 + char2 + char3;
    console.log(concatenatedString);
}

// Пример использования:
concatenateChars('a', 'b', 'c');
concatenateChars('%', '2', 'o');
concatenateChars('1', '5', 'p');